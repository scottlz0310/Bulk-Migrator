# Renovate Docker セルフホスト設定
# このファイルをコピーして .env.renovate として使用してください
# cp .env.renovate.example .env.renovate

# ============================================
# GitHub 認証設定（必須）
# ============================================
# GitHub Personal Access Token は環境変数 $GITHUB_PERSONAL_ACCESS_TOKEN から取得されます
# 
# Fine-grained token を作成（推奨）:
# Settings -> Developer settings -> Personal access tokens -> Fine-grained tokens
# 
# 必要な権限（Repository permissions）:
# - Actions: Read and write
# - Contents: Read and write
# - Issues: Read and write
# - Metadata: Read-only（自動）
# - Pull requests: Read and write
# - Workflows: Read and write
#
# 実行前に環境変数をエクスポート:
#   export GITHUB_PERSONAL_ACCESS_TOKEN="your_token_here"
#
# または ~/.bashrc に追加:
#   echo 'export GITHUB_PERSONAL_ACCESS_TOKEN="your_token_here"' >> ~/.bashrc
#   source ~/.bashrc

# GitHub リポジトリの指定（owner/repo 形式）
GITHUB_REPOSITORY=scottlz0310/Bulk-Migrator

# ============================================
# Git 設定
# ============================================
# コミット時の著者情報
GIT_AUTHOR_NAME=Renovate Bot
GIT_AUTHOR_EMAIL=renovate@example.com

# ============================================
# Renovate 実行設定
# ============================================
# ログレベル: debug, info, warn, error
LOG_LEVEL=info

# ドライラン（実際には変更を適用しない）: true または false
# 初回実行時は true にしてテストすることを推奨
DRY_RUN=false

# 設定内容を表示: true または false
PRINT_CONFIG=false

# リポジトリキャッシュ: enabled または disabled
REPOSITORY_CACHE=enabled

# ============================================
# セキュリティ設定
# ============================================
# アップグレード後のコマンド実行を無効化（セキュリティ上重要）
# この設定は docker-compose.renovate.yml で管理されます

# ============================================
# 使用方法
# ============================================
# 1. このファイルを .env.renovate にコピー:
#    cp .env.renovate.example .env.renovate
#
# 2. GitHub Personal Access Tokenを環境変数にエクスポート:
#    export GITHUB_PERSONAL_ACCESS_TOKEN="your_token_here"
#
# 3. 実行:
#    docker-compose -f docker-compose.renovate.yml --env-file .env.renovate up
#
# 初回実行時のテスト:
#    DRY_RUN=true に設定して実行し、ログを確認してください
